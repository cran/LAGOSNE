context("lagos_ingest")

# test_that("lagos_ingest works", {
#
#   skip_on_cran()
#   skip_on_travis()
#   skip_on_appveyor()
#
#   res <- lagos_ingest(LAGOSNE:::lagosne_version(),
#     limno_folder = file.path(LAGOSNE:::lagos_path(), "limno"),
#     geo_folder   = file.path(LAGOSNE:::lagos_path(), "geo"),
#     locus_folder = file.path(LAGOSNE:::lagos_path(), "locus"))
#
#   expect_equal(length(res), 3)
# })
